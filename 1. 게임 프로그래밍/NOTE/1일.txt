int main()
{
	int n;// 변수 만들기 (n 이름은 변수 이름
	// int (정수형 변수) 
	//본체는 메모리 안에 n이름이 어디에 메모리가 만들어 졌는지 알려줌
	//int 메모리 크기를 알려줌
	
	scanf("%d(정수)", &n); //키보드의 입력을 받는 것
	printf("%c(문자)", n); //화면에 출력
	charater : 캐릭터, 문자열 
	c
	return 0;
}
정수를 (준비물)
입력 받아서 (동작 1)
그대로 출력한다 (동작 2)

자료형 종류
1. 정수자료형
	char 1byte (문자표현) 아스키코드 (24 a 25 b) 메모리는 자원 아끼면 좋지  c++ c#은 예외
	int	4byte (정수만 표현) 
	unsinged(음수 제거 -) int 또는 char
	
	char 127 -127
	unsinged char 0 ~ 254 (문자 출력 안됨)
	
2. 실수자료형(수학 : 현실에 있는 실존하는 수 , 컴공에서는 실수는 소수점을 포함한 수)
%f 
float : 

좌표이동 : 벡터 미분 적분 소수점
삼각 함수를 써서 각 오브젝트를 몇도 씩 회전시켜야한다(정수로 뚝뚝, 부드러워지지)

float → int 형 변환
: 실수에서 정수로 형변환을 하면 소수점 이하 값들은 모두 사라진다

%.0f

두 정수를 입력 받아 시간 표현
입력의 조건(
int main(void)
{
	int a, b;
	scanf("%d:%d", &a, &b); // 공백으로 구분
	: 구분
	printf("%d:%d", a, b);
	return 0;
}

c
배열 
포인터 메모리 관리
자료구조	
c++ 객체 지향
	다이렉트 x 3d 
c# 게임 만들기

가고 싶은 기준
유니티 활용 능력
언리얼 엔진 활용 능력
다이렉트x 

기획 인턴
코드x -> 기획서(포폴)기초, 시스템, UI, 시장 분석서, 배틀그라운드 분석서

니가 만든 게임(모작)2dx 3d
+ 서버
+ 캐릭터 모션
완성된 게임하나  만들면 pass
@ 알고리즘 테스트
